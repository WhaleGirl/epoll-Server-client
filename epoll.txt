epoll作为多路转接的IO模型，具有LT和ET两种模式。
ET是一种高效的IO模式，减少了epoll_wait的触发次数，
创建epoll实际上就是创建epoll模型，epoll模型包括三部分
1、红黑树，注册epoll事件就是将事件添加至红黑树中，并将想关心的事件也添加进去，该红黑树以文件描述符作为key值
2、就绪队列，就绪队列里存放是就绪的文件描述符，当文件描述符就绪时会将其拷贝至就绪队列
3、回调机制,底层的一种机制


